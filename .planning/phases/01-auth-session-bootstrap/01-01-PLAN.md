---
phase: 01-auth-session-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - analysis_options.yaml
  - lib/config/env_config.dart
  - android/app/src/main/AndroidManifest.xml
  - ios/Runner/Info.plist
  - test/helpers/mocks.dart
  - test/helpers/fixtures/twitch_token_response.json
  - test/repository/services/auth/auth_token_service_test.dart
  - test/repository/services/auth/secure_storage_service_test.dart
  - test/screens/splash/cubit/splash_cubit_test.dart
  - test/screens/signIn/cubit/sign_in_cubit_test.dart
  - test/screens/auth/cubit/auth_cubit_test.dart
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06, AUTH-07]
user_setup:
  - service: twitch
    why: "Registrare Client ID per OAuth PKCE"
    env_vars:
      - name: TWITCH_CLIENT_ID
        source: "https://dev.twitch.tv/console/apps → registra app con OAuth Redirect URL `klimmeck://auth`"
    dashboard_config:
      - task: "Aggiungere redirect URI `klimmeck://auth` all'app Twitch"
        location: "Twitch Developer Console → OAuth Redirect URLs"
must_haves:
  truths:
    - "`pubspec.yaml` dichiara `flutter_web_auth_2`, `flutter_secure_storage`, `bloc_test`, `mocktail`, `crypto`"
    - "Platform manifests (Android + iOS) accettano il deep link `klimmeck://auth`"
    - "Ogni file test di Wave 0 esiste con test case marcati `skip: 'RED — waiting impl'` o fallenti per motivo giusto"
  artifacts:
    - path: "pubspec.yaml"
      provides: "Dipendenze auth runtime e dev"
      contains: "flutter_web_auth_2"
    - path: "android/app/src/main/AndroidManifest.xml"
      provides: "CallbackActivity per `klimmeck` scheme"
      contains: "flutter_web_auth_2.CallbackActivity"
    - path: "ios/Runner/Info.plist"
      provides: "URL scheme `klimmeck`"
      contains: "CFBundleURLSchemes"
    - path: "test/helpers/mocks.dart"
      provides: "Mock classes condivise (SecureStorage, Dio, FlutterWebAuth2, GraphQLClient)"
    - path: "test/repository/services/auth/auth_token_service_test.dart"
      provides: "Scheletro test per AUTH-01, AUTH-04, AUTH-06, AUTH-07"
    - path: "test/repository/services/auth/secure_storage_service_test.dart"
      provides: "Scheletro test per AUTH-02"
    - path: "test/screens/splash/cubit/splash_cubit_test.dart"
      provides: "Scheletro test per AUTH-03"
    - path: "test/screens/signIn/cubit/sign_in_cubit_test.dart"
      provides: "Scheletro test per AUTH-01 (UX)"
    - path: "test/screens/auth/cubit/auth_cubit_test.dart"
      provides: "Scheletro test per AUTH-05"
  key_links:
    - from: "pubspec.yaml"
      to: "test/helpers/mocks.dart"
      via: "mocktail dependency"
      pattern: "mocktail"
    - from: "android/app/src/main/AndroidManifest.xml"
      to: "deep link klimmeck://auth"
      via: "intent-filter"
      pattern: "android:scheme=\"klimmeck\""
---

<objective>
Wave 0 per Phase 1: installare dipendenze auth (runtime + dev), configurare platform manifests per il deep link `klimmeck://auth`, esporre `TWITCH_CLIENT_ID` via `EnvConfig`, abilitare `avoid_print` e creare gli scheletri di test TDD (RED) che le wave successive faranno diventare GREEN.

Purpose: Senza questo setup non è possibile né compilare (deep link nativo richiesto da flutter_web_auth_2), né applicare TDD (bloc_test/mocktail non in pubspec), né scrivere il primo test rosso su `SecureStorageService` o `AuthTokenService`.
Output: pubspec aggiornato, manifests aggiornati, EnvConfig esteso, 5 file test scheletro + helpers mocks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auth-session-bootstrap/01-CONTEXT.md
@.planning/phases/01-auth-session-bootstrap/01-RESEARCH.md
@.planning/phases/01-auth-session-bootstrap/01-VALIDATION.md
@docs/rules/workflow.md
@docs/rules/testing.md
@docs/rules/naming.md
@pubspec.yaml
@lib/config/env_config.dart

<interfaces>
<!-- Env config esistente (da estendere). Leggere file prima di modificare. -->

From lib/config/env_config.dart (da confermare con Read durante execution):
```dart
class EnvConfig {
  static const String graphqlHttpUrl = String.fromEnvironment('GRAPHQL_HTTP_URL');
  static const String graphqlWsUrl = String.fromEnvironment('GRAPHQL_WS_URL');
  static const int wsInactivityTimeoutSeconds = int.fromEnvironment('WS_INACTIVITY_TIMEOUT', defaultValue: 30);
  static const int queryTimeoutSeconds = int.fromEnvironment('QUERY_TIMEOUT', defaultValue: 10);
  // + altre costanti
}
```

Aggiungere (firma esatta):
```dart
static const String twitchClientId = String.fromEnvironment('TWITCH_CLIENT_ID');
static const String twitchAuthUrl = 'https://id.twitch.tv/oauth2/authorize';
static const String twitchTokenUrl = 'https://id.twitch.tv/oauth2/token';
static const String twitchValidateUrl = 'https://id.twitch.tv/oauth2/validate';
static const String twitchRevokeUrl = 'https://id.twitch.tv/oauth2/revoke';
static const String authRedirectUri = 'klimmeck://auth';
static const String authCallbackScheme = 'klimmeck';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aggiungere dipendenze auth + dev dependencies + crypto</name>
  <read_first>
    - pubspec.yaml (intera)
    - .planning/phases/01-auth-session-bootstrap/01-RESEARCH.md §Standard Stack, §Environment Availability
    - docs/rules/workflow.md §Branch (verificare che siamo su `feat/auth-session-bootstrap`)
  </read_first>
  <files>pubspec.yaml</files>
  <action>
    Prima di qualsiasi modifica: `git checkout develop && git pull && git checkout -b feat/auth-session-bootstrap` se il branch non esiste (regola invalicabile workflow #3).

    In `pubspec.yaml` aggiungere sotto `dependencies:` (mantenere ordinamento per raggruppamento commentato esistente; creare gruppo `#Auth`):
    ```yaml
      #Auth
      flutter_web_auth_2: ^5.0.2
      flutter_secure_storage: ^10.0.0
      crypto: ^3.0.3
    ```
    Sotto `dev_dependencies:` aggiungere:
    ```yaml
      bloc_test: ^9.1.7
      mocktail: ^1.0.4
    ```
    Eseguire `flutter pub get` per risolvere. Se uno dei vincoli di versione non risolve (conflitto SDK), documentare in commit message la versione effettivamente risolta (mai usare `any`).
  </action>
  <verify>
    <automated>grep -q "flutter_web_auth_2:" pubspec.yaml && grep -q "flutter_secure_storage:" pubspec.yaml && grep -q "crypto:" pubspec.yaml && grep -q "bloc_test:" pubspec.yaml && grep -q "mocktail:" pubspec.yaml && flutter pub get</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "flutter_web_auth_2:" pubspec.yaml` ≥ 1
    - `grep -c "flutter_secure_storage:" pubspec.yaml` ≥ 1
    - `grep -c "crypto:" pubspec.yaml` ≥ 1
    - `grep -c "bloc_test:" pubspec.yaml` ≥ 1
    - `grep -c "mocktail:" pubspec.yaml` ≥ 1
    - `flutter pub get` exit code 0
    - `pubspec.lock` contiene `flutter_web_auth_2` e `flutter_secure_storage`
  </acceptance_criteria>
  <done>5 package dichiarati; `flutter pub get` risolve; branch `feat/auth-session-bootstrap` attivo.</done>
</task>

<task type="auto">
  <name>Task 2: Platform manifests + EnvConfig + analysis_options</name>
  <read_first>
    - android/app/src/main/AndroidManifest.xml (intero)
    - ios/Runner/Info.plist (intero)
    - lib/config/env_config.dart (intero)
    - analysis_options.yaml (intero)
    - .planning/phases/01-auth-session-bootstrap/01-RESEARCH.md §Environment Availability (blocchi Android/iOS)
  </read_first>
  <files>
    android/app/src/main/AndroidManifest.xml,
    ios/Runner/Info.plist,
    lib/config/env_config.dart,
    analysis_options.yaml
  </files>
  <action>
    1. `android/app/src/main/AndroidManifest.xml` — all'interno del tag `<application>`, aggiungere esattamente:
    ```xml
    <activity
      android:name="com.linusu.flutter_web_auth_2.CallbackActivity"
      android:exported="true">
      <intent-filter android:label="flutter_web_auth_2">
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="klimmeck" android:host="auth" />
      </intent-filter>
    </activity>
    ```

    2. `ios/Runner/Info.plist` — aggiungere (se `CFBundleURLTypes` esiste, estendere l'array; altrimenti crearlo):
    ```xml
    <key>CFBundleURLTypes</key>
    <array>
      <dict>
        <key>CFBundleTypeRole</key><string>Editor</string>
        <key>CFBundleURLName</key><string>klimmeck.auth</string>
        <key>CFBundleURLSchemes</key>
        <array><string>klimmeck</string></array>
      </dict>
    </array>
    ```

    3. `lib/config/env_config.dart` — aggiungere le costanti esatte dichiarate in `<interfaces>` sopra (Twitch endpoints + redirect URI + callback scheme + `twitchClientId` via `String.fromEnvironment`). NON hardcodare il client ID nel codice; deve venire da `--dart-define=TWITCH_CLIENT_ID=...`.

    4. `analysis_options.yaml` — abilitare la regola `avoid_print: true` sotto `linter.rules:`. Se `linter.rules` non esiste, crearla. Correggere nello stesso commit eventuali `print(...)` pre-esistenti (usare `debugPrint` da `package:flutter/foundation.dart` o rimuovere) — questo è Boy Scout Rule obbligatoria (CLAUDE.md).
  </action>
  <verify>
    <automated>grep -q "com.linusu.flutter_web_auth_2.CallbackActivity" android/app/src/main/AndroidManifest.xml && grep -q "klimmeck" android/app/src/main/AndroidManifest.xml && grep -q "CFBundleURLSchemes" ios/Runner/Info.plist && grep -q "twitchClientId" lib/config/env_config.dart && grep -q "authCallbackScheme" lib/config/env_config.dart && grep -q "avoid_print" analysis_options.yaml && flutter analyze</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "android:scheme=\"klimmeck\"" android/app/src/main/AndroidManifest.xml` ≥ 1
    - `grep -c "<string>klimmeck</string>" ios/Runner/Info.plist` ≥ 1
    - `grep -c "TWITCH_CLIENT_ID" lib/config/env_config.dart` ≥ 1
    - `grep -c "twitchValidateUrl" lib/config/env_config.dart` ≥ 1
    - `grep -c "klimmeck://auth" lib/config/env_config.dart` ≥ 1
    - `grep -c "avoid_print" analysis_options.yaml` ≥ 1
    - `flutter analyze` exit code 0 (zero warning, zero error)
  </acceptance_criteria>
  <done>Deep link registrato su entrambe le piattaforme; EnvConfig espone le 7 costanti auth; `flutter analyze` pulito.</done>
</task>

<task type="auto">
  <name>Task 3: Scheletri test TDD (RED) + helpers/mocks</name>
  <read_first>
    - docs/rules/testing.md (intero)
    - docs/rules/state-management.md §Testing
    - .planning/phases/01-auth-session-bootstrap/01-VALIDATION.md §Wave 0 Requirements
    - .planning/phases/01-auth-session-bootstrap/01-RESEARCH.md §Validation Architecture
    - docs/rules/naming.md §Test
  </read_first>
  <files>
    test/helpers/mocks.dart,
    test/helpers/fixtures/twitch_token_response.json,
    test/repository/services/auth/auth_token_service_test.dart,
    test/repository/services/auth/secure_storage_service_test.dart,
    test/screens/splash/cubit/splash_cubit_test.dart,
    test/screens/signIn/cubit/sign_in_cubit_test.dart,
    test/screens/auth/cubit/auth_cubit_test.dart
  </files>
  <action>
    1. `test/helpers/mocks.dart` — definire con `mocktail` classi mock riutilizzabili:
    ```dart
    import 'package:dio/dio.dart';
    import 'package:flutter_secure_storage/flutter_secure_storage.dart';
    import 'package:graphql_flutter/graphql_flutter.dart';
    import 'package:mocktail/mocktail.dart';

    class MockFlutterSecureStorage extends Mock implements FlutterSecureStorage {}
    class MockDio extends Mock implements Dio {}
    class MockGraphQLClient extends Mock implements GraphQLClient {}
    // Placeholder per wrapper futuri — MockSecureStorageService, MockAuthTokenService — aggiunti nelle wave successive.
    ```
    Registrare i fallback value necessari (`registerFallbackValue(Uri())`, ecc.) in una funzione `void registerAuthFallbacks()` esportata.

    2. `test/helpers/fixtures/twitch_token_response.json` — fixture payload realistico:
    ```json
    {
      "access_token": "fake_access_token_abc123",
      "expires_in": 14400,
      "refresh_token": "fake_refresh_token_xyz789",
      "scope": ["user:read:email"],
      "token_type": "bearer"
    }
    ```

    3. Per ciascuno dei 5 file test, creare struttura `main()` con `group(...)` e `test(...)` marcati `skip: 'RED — implementation pending in plan 01-0N'` per CIASCUN requirement copertto. Obbligatorio usare `blocTest` signature per cubit test (anche se skipped) per garantire che le imports siano validi.

    `auth_token_service_test.dart` — test skippati per:
      - `AUTH-01: generates OAuth URL with PKCE S256 and force_verify=true`
      - `AUTH-02: persists tokens only via SecureStorage, never SharedPreferences`
      - `AUTH-04: logout teardown executes D-12 order (revoke → cancel subs → recreate client → clear storage → emit Unauthenticated)`
      - `AUTH-06: concurrent getAccessToken() calls trigger exactly one refresh via Completer mutex`
      - `AUTH-07: refresh returning invalid_grant triggers handleRevocation() and emits Unauthenticated`
      - `AUTH-03: cold-start validate with valid refresh token emits Authenticated`
      - `AUTH-07 cold-start: validate 401 → emits Unauthenticated with SessionExpired cause`

    `secure_storage_service_test.dart` — test skippati per:
      - `AUTH-02: saveTokens writes access + refresh under fixed keys`
      - `AUTH-02: clearAll deletes both keys`
      - `AUTH-02: getRefreshToken returns null when absent`

    `splash_cubit_test.dart` — test skippati per:
      - `AUTH-03: emits SplashAuthenticated when AuthTokenService reports valid session`
      - `AUTH-03: emits SplashUnauthenticated when no refresh token`
      - `D-18: emits SplashNetworkDelayed after 10s without resolution`
      - `AUTH-07: emits SplashSessionExpired on revocation signal`

    `sign_in_cubit_test.dart` — test skippati per:
      - `AUTH-01: login() delegates to AuthTokenService.login and emits SignInInProgress → SignInSuccess`
      - `D-21: PlatformException(CANCELED) is swallowed, state returns to SignInIdle silently`
      - `D-22: network error emits SignInError with inline message`

    `auth_cubit_test.dart` — test skippati per:
      - `AUTH-04: logout() emits AuthLoggingOut → Unauthenticated`
      - `AUTH-05: switchAccount() performs logout then login with force_verify=true`

    Commit messaggio: `test(auth): add Wave 0 TDD skeletons for AUTH-01..07`.
  </action>
  <verify>
    <automated>flutter test --reporter=compact 2>&1 | tee /tmp/phase01_w0.log && grep -q "All tests passed" /tmp/phase01_w0.log && test -f test/helpers/mocks.dart && test -f test/helpers/fixtures/twitch_token_response.json</automated>
  </verify>
  <acceptance_criteria>
    - Tutti i file test elencati in `<files>` esistono
    - `flutter test` exit code 0 (test marcati `skip` contano come pass)
    - `grep -rn "AUTH-01" test/` ≥ 2 occorrenze
    - `grep -rn "AUTH-07" test/` ≥ 2 occorrenze
    - `grep -rn "skip: 'RED" test/` ≥ 15 occorrenze (≥ 1 per test case skippato)
    - `grep -q "MockFlutterSecureStorage" test/helpers/mocks.dart`
    - `grep -q "MockDio" test/helpers/mocks.dart`
    - `flutter analyze` exit code 0
  </acceptance_criteria>
  <done>Tutti gli scheletri TDD compilano e sono skippati con ragione "RED — implementation pending". Gli helper mocks sono pronti per le wave successive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine → repo | Client ID non deve entrare in VCS |
| OS → app (deep link) | `klimmeck://auth` callback può essere hijacked da altre app installate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Information Disclosure | `TWITCH_CLIENT_ID` | mitigate | Passato solo via `--dart-define`; nessun `.env` in repo; `env_config.dart` usa `String.fromEnvironment`, mai literal |
| T-01-01-02 | Spoofing | Deep link `klimmeck://auth` | accept | Mitigazione reale è PKCE S256 (plan 02): intercettare il callback non permette scambio code→token senza `code_verifier`. Manifest qui registra solo lo scheme |
| T-01-01-03 | Tampering | pubspec.lock | accept | Versioni bloccate via `^x.y.z` + commit del lockfile; supply-chain audit fuori scope Phase 1 (HARDEN-xx) |
</threat_model>

<verification>
- `flutter pub get` risolve senza conflitti
- `flutter analyze` pulito
- `flutter test` esegue senza crash (tutti skip o pass)
- Branch `feat/auth-session-bootstrap` attivo e pubblicato origin
- Deep link scheme registrato su Android + iOS manifests
</verification>

<success_criteria>
- 5 package installati (3 runtime + 2 dev + crypto)
- Android + iOS manifests accettano `klimmeck://auth`
- `EnvConfig` espone 7 costanti auth (`twitchClientId`, `twitchAuthUrl`, `twitchTokenUrl`, `twitchValidateUrl`, `twitchRevokeUrl`, `authRedirectUri`, `authCallbackScheme`)
- 5 file test scheletro + `mocks.dart` + fixture JSON esistono
- Ogni requirement AUTH-01..07 ha ≥ 1 test case (skipped RED) che lo referenzia esplicitamente nel nome
- `analysis_options.yaml` abilita `avoid_print`
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-session-bootstrap/01-01-SUMMARY.md`
</output>
