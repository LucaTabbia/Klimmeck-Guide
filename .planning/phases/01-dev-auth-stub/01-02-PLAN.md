---
phase: 01-dev-auth-stub
plan: 02
type: execute
wave: 1
depends_on: [01-01-PLAN]
files_modified:
  - lib/repository/services/auth/auth_token_service.dart
  - lib/repository/services/auth/dev_auth_token_service.dart
  - lib/repository/services/auth/auth.dart
  - lib/config/env_config.dart
autonomous: true
requirements:
  - DEV-AUTH-01
  - DEV-AUTH-02
  - DEV-AUTH-03
  - DEV-AUTH-04
must_haves:
  truths:
    - "Una classe `AuthTokenService` (abstract) espone il contratto canonico di Phase 11 (D-01, D-02 CONTEXT.md)"
    - "`AuthState` è una sealed class con 3 sottotipi: `AuthBootstrapping`, `AuthAuthenticated(user, accessToken)`, `AuthUnauthenticated`"
    - "`DevAuthTokenService` implementa `AuthTokenService` e, dopo `initialize()`, lo stream emette `AuthBootstrapping` → `AuthAuthenticated` con valori da `.env`"
    - "`getAccessToken()` ritorna il valore di `DEV_AUTH_ACCESS_TOKEN` da dotenv"
    - "`DEV_AUTH_ROLE` ∈ {guard, adventurer, innkeeper} produce `AuthAuthenticated.user.role` corrispondente; valore sconosciuto → fallback `adventurer` con warning log"
    - "`login()`, `logout()`, `handleRevocation()` sono no-op con `debugPrint` solo in `kDebugMode`; non emettono nuovi stati"
    - "Tutti i test RED di Plan 01 (auth/*) passano (GREEN)"
  artifacts:
    - path: "lib/repository/services/auth/auth_token_service.dart"
      provides: "Contratto pubblico abstract + sealed AuthState"
      exports: ["AuthTokenService", "AuthState", "AuthBootstrapping", "AuthAuthenticated", "AuthUnauthenticated"]
      min_lines: 50
    - path: "lib/repository/services/auth/dev_auth_token_service.dart"
      provides: "Implementazione stub backed da flutter_dotenv"
      exports: ["DevAuthTokenService"]
      min_lines: 70
    - path: "lib/repository/services/auth/auth.dart"
      provides: "Barrel export"
      contains: "export"
    - path: "lib/config/env_config.dart"
      provides: "Aggiunta flag `devAuthEnabled` come getter runtime via dotenv"
      contains: "devAuthEnabled"
  key_links:
    - from: "lib/repository/services/auth/dev_auth_token_service.dart"
      to: "flutter_dotenv dotenv.env"
      via: "initialize() + getAccessToken()"
      pattern: "dotenv\\.env\\['DEV_AUTH_"
    - from: "lib/repository/services/auth/dev_auth_token_service.dart"
      to: "lib/repository/services/auth/auth_token_service.dart"
      via: "extends AuthTokenService"
      pattern: "extends AuthTokenService"
---

<objective>
Definire il **contratto** `AuthTokenService` (abstract class + sealed `AuthState`) e la sua prima implementazione concreta `DevAuthTokenService` che legge `.env` via `flutter_dotenv` ed emette immediatamente `AuthAuthenticated` con un test user. Nessun wiring in `main.dart` in questo plan (è Plan 03). Tutti i test RED di Plan 01 (in `test/repository/services/auth/`) passano a GREEN.

Purpose: Stabilire il contratto canonico su cui Phase 11 atterrerà per sostituzione (D-02 CONTEXT.md). La fedeltà al contratto è il vincolo più critico della fase: qualunque cosa qui venga pubblicata, Phase 11 la rispetterà senza toccare i consumer (GraphQL, dio, Cubit).

Output: 3 file sorgente (`auth_token_service.dart`, `dev_auth_token_service.dart`, `auth.dart` barrel) + 1 aggiornamento a `env_config.dart`. 4 test file GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dev-auth-stub/01-CONTEXT.md
@.planning/phases/01-dev-auth-stub/01-RESEARCH.md
@.planning/phases/11-auth-session-bootstrap/11-CONTEXT.md
@docs/rules/architecture.md
@docs/rules/naming.md
@docs/rules/state-management.md
@docs/rules/testing.md
@lib/models/user.dart
@lib/models/enums/role_type.dart
@lib/config/env_config.dart
@test/helpers/auth_fixtures.dart
@test/helpers/fixtures/dev_auth_env.dart

<interfaces>
<!-- Source of truth: 11-CONTEXT.md D-01, D-02, D-03 -->

`AuthTokenService` public surface (abstract):
```dart
Stream<AuthState> get authStateStream;         // broadcast
Future<String?> getAccessToken();              // nullable
Future<void> login();
Future<void> logout();
Future<void> handleRevocation();
void dispose();                                // chiude StreamController (pitfall 5 RESEARCH)
```

`AuthState` sealed:
```dart
sealed class AuthState extends Equatable { const AuthState(); }
final class AuthBootstrapping extends AuthState { ... }
final class AuthAuthenticated extends AuthState { final User user; final String accessToken; }
final class AuthUnauthenticated extends AuthState { ... }
```

User constructor (esistente):
```dart
User({ required id, required twitchId, required twitchPoints, required currentCharacter, required role })
```

`RoleType` esistente: `{ guard, adventurer, innkeeper }`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Definire contratto `AuthTokenService` + sealed `AuthState`</name>
  <read_first>
    - test/repository/services/auth/auth_token_service_contract_test.dart (stub RED da Plan 01)
    - .planning/phases/11-auth-session-bootstrap/11-CONTEXT.md §D-02
    - docs/rules/naming.md (PascalCase, sealed naming)
    - docs/rules/state-management.md (Equatable, immutabilità)
    - lib/models/user.dart
  </read_first>
  <behavior>
    - Il test `auth_token_service_contract_test.dart` (già RED) passa.
    - `DevAuthTokenService()` è staticamente assegnabile a `AuthTokenService`.
    - `AuthState` è sealed con esattamente 3 sottotipi final.
    - `AuthAuthenticated(user: u, accessToken: t).props` include `user` e `accessToken` (equatable-safe).
    - `AuthBootstrapping()` e `AuthUnauthenticated()` sono const-costruibili; istanze uguali `==` true.
  </behavior>
  <files>
    lib/repository/services/auth/auth_token_service.dart,
    lib/repository/services/auth/auth.dart
  </files>
  <action>
1. Creare `lib/repository/services/auth/auth_token_service.dart`:
   - Import: `package:equatable/equatable.dart`, `package:klimmeck_guide/models/user.dart`.
   - `sealed class AuthState extends Equatable { const AuthState(); @override List<Object?> get props => []; }`.
   - `final class AuthBootstrapping extends AuthState { const AuthBootstrapping(); }` — no extra props.
   - `final class AuthAuthenticated extends AuthState { const AuthAuthenticated({required this.user, required this.accessToken}); final User user; final String accessToken; @override List<Object?> get props => [user, accessToken]; }`.
   - `final class AuthUnauthenticated extends AuthState { const AuthUnauthenticated(); }`.
   - `abstract class AuthTokenService { Stream<AuthState> get authStateStream; Future<String?> getAccessToken(); Future<void> login(); Future<void> logout(); Future<void> handleRevocation(); void dispose(); }`.
   - Doc comment dartdoc su ogni membro che richiami Phase 11 / Phase 1 behaviour (copiato/adattato da RESEARCH.md Pattern 1).

2. Creare barrel `lib/repository/services/auth/auth.dart` con:
   ```dart
   export 'auth_token_service.dart';
   export 'dev_auth_token_service.dart';
   ```
   (il secondo export sarà risolvibile dopo Task 2.)

3. NIENTE implementazione concreta qui. Solo contratto.

Implementa DEV-AUTH-01. Rispetta D-02 CONTEXT.md §"Implementation Decisions".
  </action>
  <verify>
    <automated>flutter analyze lib/repository/services/auth/auth_token_service.dart &amp;&amp; dart run test/repository/services/auth/auth_token_service_contract_test.dart || flutter test test/repository/services/auth/auth_token_service_contract_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/repository/services/auth/auth_token_service.dart` esiste
    - `AuthState` è dichiarata `sealed` con 3 sottotipi `final`
    - `AuthAuthenticated` ha campi `user: User` e `accessToken: String` entrambi `final`
    - `AuthTokenService` è `abstract` con 5 metodi + `dispose()`
    - `flutter analyze` su quel file pulito
    - Il test `auth_token_service_contract_test.dart` GREEN dopo Task 2 (compile-time assignability verificata alla fine di Task 2)
  </acceptance_criteria>
  <done>Contratto pubblico definito, barrel creato, `flutter analyze` pulito.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementare `DevAuthTokenService` (stub .env)</name>
  <read_first>
    - test/repository/services/auth/dev_auth_token_service_test.dart (RED)
    - test/repository/services/auth/dev_auth_token_service_role_test.dart (RED)
    - test/repository/services/auth/dev_auth_token_service_noop_test.dart (RED)
    - test/helpers/fixtures/dev_auth_env.dart
    - test/helpers/auth_fixtures.dart
    - lib/repository/services/auth/auth_token_service.dart (appena creato)
    - .planning/phases/01-dev-auth-stub/01-RESEARCH.md §"Pattern 2" + §"Common Pitfalls" 1, 4, 5
    - lib/models/user.dart, lib/models/enums/role_type.dart
  </read_first>
  <behavior>
    - `initialize()` emette nell'ordine: `AuthBootstrapping` → `AuthAuthenticated(user: User(...), accessToken: env.DEV_AUTH_ACCESS_TOKEN)`.
    - `User.id == env.DEV_AUTH_USER_ID`, `User.twitchId == env.DEV_AUTH_TWITCH_ID`, `User.twitchPoints == 0`, `User.currentCharacter == null`, `User.role` da `DEV_AUTH_ROLE` normalizzato lowercase.
    - `DEV_AUTH_ROLE` sconosciuto/malformato → `RoleType.adventurer` + `debugPrint` warning in `kDebugMode`.
    - `getAccessToken()` ritorna `dotenv.env['DEV_AUTH_ACCESS_TOKEN']` (può essere null se mancante).
    - `login()`, `logout()`, `handleRevocation()` completano senza emettere nuovi `AuthState` e senza eccezioni; in `kDebugMode` stampano `debugPrint('[DevAuth] <method>() — no-op in dev stub')`.
    - `dispose()` chiude lo `StreamController`; chiamate subsequent a `authStateStream.listen` NON sono aspettate a funzionare (documentato).
    - `authStateStream` è broadcast (supporta multiple listener: interceptor + SplashCubit + GraphQL auth link).
  </behavior>
  <files>lib/repository/services/auth/dev_auth_token_service.dart</files>
  <action>
1. Creare `lib/repository/services/auth/dev_auth_token_service.dart`:
   ```dart
   import 'dart:async';
   import 'package:flutter/foundation.dart';
   import 'package:flutter_dotenv/flutter_dotenv.dart';
   import 'package:klimmeck_guide/models/enums/role_type.dart';
   import 'package:klimmeck_guide/models/user.dart';
   import 'auth_token_service.dart';

   class DevAuthTokenService extends AuthTokenService {
     DevAuthTokenService() : _controller = StreamController<AuthState>.broadcast();

     final StreamController<AuthState> _controller;

     @override
     Stream<AuthState> get authStateStream => _controller.stream;

     Future<void> initialize() async {
       _controller.add(const AuthBootstrapping());
       final accessToken = dotenv.env['DEV_AUTH_ACCESS_TOKEN'] ?? '';
       final userId = dotenv.env['DEV_AUTH_USER_ID'] ?? '';
       final twitchId = dotenv.env['DEV_AUTH_TWITCH_ID'] ?? '';
       final role = _parseRole(dotenv.env['DEV_AUTH_ROLE']);
       final user = User(
         id: userId,
         twitchId: twitchId,
         twitchPoints: 0,
         currentCharacter: null,
         role: role,
       );
       _controller.add(AuthAuthenticated(user: user, accessToken: accessToken));
     }

     RoleType _parseRole(String? raw) {
       if (raw == null) return RoleType.adventurer;
       final normalized = raw.trim().toLowerCase();
       try {
         return RoleType.values.byName(normalized);
       } catch (_) {
         if (kDebugMode) {
           debugPrint('[DevAuth] unknown role "$raw", falling back to adventurer');
         }
         return RoleType.adventurer;
       }
     }

     @override
     Future<String?> getAccessToken() async => dotenv.env['DEV_AUTH_ACCESS_TOKEN'];

     @override
     Future<void> login() async {
       if (kDebugMode) debugPrint('[DevAuth] login() — no-op in dev stub');
     }

     @override
     Future<void> logout() async {
       if (kDebugMode) debugPrint('[DevAuth] logout() — no-op in dev stub');
     }

     @override
     Future<void> handleRevocation() async {
       if (kDebugMode) debugPrint('[DevAuth] handleRevocation() — no-op in dev stub');
     }

     @override
     void dispose() {
       _controller.close();
     }
   }
   ```

2. Seguire regole `architecture.md`: nessun import da `package:flutter/material.dart` (solo `foundation.dart` per `kDebugMode`/`debugPrint`). Nessun `BuildContext`. Nessuna dipendenza da repository/cubit.

3. Iterare sui 3 test file RED fino a GREEN. Se un test richiede un dettaglio non presente nel comportamento sopra (es. check su stream broadcast multi-listener), allinearsi al test (il test è source of truth per TDD).

Implementa DEV-AUTH-02, DEV-AUTH-03, DEV-AUTH-04. Rispetta D-04 (no secure storage), D-05 (flag `DEV_AUTH_ENABLED` non letto qui — letto in Plan 03 `main.dart`).
  </action>
  <verify>
    <automated>flutter test test/repository/services/auth/dev_auth_token_service_test.dart test/repository/services/auth/dev_auth_token_service_role_test.dart test/repository/services/auth/dev_auth_token_service_noop_test.dart test/repository/services/auth/auth_token_service_contract_test.dart &amp;&amp; flutter analyze lib/repository/services/auth/</automated>
  </verify>
  <acceptance_criteria>
    - Tutti e 4 i test file `test/repository/services/auth/*.dart` GREEN
    - `flutter analyze lib/repository/services/auth/` pulito
    - `DevAuthTokenService extends AuthTokenService` verificato staticamente
    - Role fallback su valore sconosciuto: test `role_test.dart` verifica il caso `'UnknownRole'` → `RoleType.adventurer`
    - `login/logout/handleRevocation` NON emettono stati (verificato in `noop_test.dart`)
    - `StreamController` è broadcast (multiple listener supportati senza `StateError`)
  </acceptance_criteria>
  <done>Stub funzionante, 4 test GREEN, analyze pulito.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Aggiungere flag `devAuthEnabled` a `EnvConfig`</name>
  <read_first>
    - lib/config/env_config.dart
    - .planning/phases/01-dev-auth-stub/01-RESEARCH.md §"Pattern 4" + §"State of the Art"
    - .planning/phases/01-dev-auth-stub/01-CONTEXT.md §D-05
  </read_first>
  <files>lib/config/env_config.dart</files>
  <action>
1. Aggiungere a `EnvConfig` (classe esistente con `EnvConfig._();`) un **getter statico runtime** (NON `const`, perché `dotenv` è runtime):
   ```dart
   /// Runtime flag (non const) letto da .env via flutter_dotenv.
   /// In release build con `.env` rimosso dagli assets, ritorna false.
   /// Phase 11 rimuoverà questo flag insieme a DevAuthTokenService.
   static bool get devAuthEnabled {
     try {
       return dotenv.env['DEV_AUTH_ENABLED']?.toLowerCase() == 'true';
     } catch (_) {
       // dotenv non ancora inizializzato (es. test che non chiama loadTestEnv): fail safe
       return false;
     }
   }
   ```
2. Aggiungere `import 'package:flutter_dotenv/flutter_dotenv.dart';` in cima al file.
3. Lasciare inalterati tutti gli altri campi `const String.fromEnvironment(...)` — sono compile-time e restano tali.
4. Aggiungere doc comment sulla sezione "Dev Auth" che spiega perché è runtime invece di `fromEnvironment` (pitfall 1 RESEARCH.md).

Implementa D-05 (flag `DEV_AUTH_ENABLED`). Boy Scout Rule: se il file ha sezioni non ordinate, opzionalmente ordinare (solo se resta in scope).
  </action>
  <verify>
    <automated>flutter analyze lib/config/env_config.dart &amp;&amp; grep -q "devAuthEnabled" lib/config/env_config.dart &amp;&amp; grep -q "dotenv" lib/config/env_config.dart</automated>
  </verify>
  <acceptance_criteria>
    - `EnvConfig.devAuthEnabled` esiste come getter statico runtime (non `const`)
    - Ritorna `true` solo se `dotenv.env['DEV_AUTH_ENABLED'] == 'true'` (case-insensitive)
    - Safe su dotenv non inizializzato (ritorna `false` invece di lanciare)
    - `flutter analyze` pulito
  </acceptance_criteria>
  <done>Flag disponibile per il factory in `main.dart` (Plan 03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env` (disk) → `DevAuthTokenService` | Untrusted input crossa qui: chiavi arbitrarie, valori arbitrari, possibile role injection (`DEV_AUTH_ROLE=innkeeper` locally) |
| `DevAuthTokenService` → downstream consumer (graphql/dio) | Token attraversa il boundary; deve rispettare il contratto (solo string in-memory, no persistenza) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | `DEV_AUTH_ROLE` in `.env` forzato a `innkeeper` da un dev per bypassare gating | accept | Phase 1 è dev-only. CLAUDE.md §4: il backend è fonte di verità — ogni admin mutation è enforced server-side (ADMIN-07, HARDEN-02). Il client-side role switching è UX-only per test manuali. |
| T-01-02-02 | Information Disclosure | `debugPrint` di token o user id in log release | mitigate | Tutti i log sono wrappati in `if (kDebugMode) { debugPrint(...) }`. Nessun log stampa il token (solo il nome del metodo no-op). Verificato via `grep 'accessToken' lib/repository/services/auth/dev_auth_token_service.dart` → nessun match in debugPrint |
| T-01-02-03 | Denial of Service | `RoleType.values.byName()` su input malformato lancia `ArgumentError` non gestita, crasha app al bootstrap | mitigate | `_parseRole()` wrappa in try/catch con fallback a `RoleType.adventurer` (pitfall 4 RESEARCH.md) |
| T-01-02-04 | Information Disclosure | `AuthAuthenticated.accessToken` esposto in `props` di Equatable → potrebbe apparire in `toString()` dei test | accept | Rischio solo in log di test (dev-only); in produzione `DevAuthTokenService` non esiste (Phase 12 audit verifica rimozione, HARDEN-01/06) |
| T-01-02-05 | Repudiation | Nessun audit log degli switch di role via `.env` | accept | Dev-only; Phase 10/11 introducono audit server-side per mutations reali (ADMIN-08) |
</threat_model>

<verification>
- `flutter test test/repository/services/auth/` tutti GREEN
- `flutter analyze lib/repository/services/auth/ lib/config/env_config.dart` pulito
- `DevAuthTokenService` non importa `package:flutter/material.dart` (SoC: no UI deps in service layer)
- `grep -r 'DevAuthTokenService' lib/` mostra solo il file sorgente (no wiring in main.dart ancora — è Plan 03)
- `AuthTokenService` non ha implementazioni concrete oltre `DevAuthTokenService` in questo plan
</verification>

<success_criteria>
- DEV-AUTH-01: contratto pubblico `AuthTokenService` identico a quello di Phase 11 (11-CONTEXT.md D-02)
- DEV-AUTH-02: `DevAuthTokenService.initialize()` legge `.env` e emette `AuthAuthenticated` con `User` completo + access token
- DEV-AUTH-03: role switching via `DEV_AUTH_ROLE` funziona per tutti e 3 i `RoleType` + fallback sicuro
- DEV-AUTH-04: login/logout/handleRevocation no-op con warning debug-only
- Tutti i 4 test unit della directory `test/repository/services/auth/` GREEN
- `flutter analyze` pulito su lib/repository/services/auth/
</success_criteria>

<output>
After completion, create `.planning/phases/01-dev-auth-stub/01-02-SUMMARY.md` con:
- Firma pubblica di `AuthTokenService` (copia dartdoc)
- Firma di `AuthState` sealed + 3 sottotipi
- Conferma mappatura env → User + role fallback
- Lista test GREEN
- Note per Plan 03 sui punti di wiring (RepositoryProvider sopra MultiBlocProvider in main.dart, GraphQL auth link, dio interceptor)
</output>
