---
phase: 01-dev-auth-stub
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pubspec.yaml
  - .env.example
  - .env
  - .gitignore
  - test/helpers/auth_fixtures.dart
  - test/helpers/fixtures/dev_auth_env.dart
  - test/repository/services/auth/auth_token_service_contract_test.dart
  - test/repository/services/auth/dev_auth_token_service_test.dart
  - test/repository/services/auth/dev_auth_token_service_role_test.dart
  - test/repository/services/auth/dev_auth_token_service_noop_test.dart
  - test/app/app_wiring_test.dart
  - test/network/auth_interceptor_test.dart
  - test/network/graphql_auth_link_test.dart
autonomous: true
requirements:
  - DEV-AUTH-01
  - DEV-AUTH-02
  - DEV-AUTH-03
  - DEV-AUTH-04
  - DEV-AUTH-05
must_haves:
  truths:
    - "`flutter pub get` completa senza errori dopo aggiunta dipendenze"
    - "Il file `.env.example` documenta le 5 variabili DEV_AUTH_* con placeholder sicuri"
    - "Il file `.env` reale non è tracciato da git (`.gitignore` lo esclude)"
    - "`.env` è dichiarato come Flutter asset in `pubspec.yaml`"
    - "Tutti i file test stub esistono e compilano (red state TDD — falliscono perché l'implementazione manca)"
    - "`flutter test` eseguito restituisce failure con messaggi significativi sui test stub (non errori di compilazione)"
  artifacts:
    - path: "pubspec.yaml"
      provides: "flutter_dotenv 6.0.0 runtime + bloc_test 10.0.0 + mocktail 1.0.5 dev_dependencies + asset .env"
      contains: "flutter_dotenv"
    - path: ".env.example"
      provides: "Template variabili DEV_AUTH_* committed in VCS"
      contains: "DEV_AUTH_ENABLED"
    - path: ".env"
      provides: "File env locale (gitignored) con valori dev reali"
    - path: ".gitignore"
      provides: "Regola esclusione `.env`"
      contains: ".env"
    - path: "test/helpers/auth_fixtures.dart"
      provides: "testUser, testAccessToken, testUserId, testTwitchId fixtures condivisi"
    - path: "test/helpers/fixtures/dev_auth_env.dart"
      provides: "`loadTestEnv()` helper per inizializzare dotenv con valori test senza file reale"
    - path: "test/repository/services/auth/auth_token_service_contract_test.dart"
      provides: "Test stub contratto (RED — verifica 5 metodi + AuthState sealed)"
    - path: "test/repository/services/auth/dev_auth_token_service_test.dart"
      provides: "Test stub init+getAccessToken (RED)"
    - path: "test/repository/services/auth/dev_auth_token_service_role_test.dart"
      provides: "Test stub role switching parametrico (RED)"
    - path: "test/repository/services/auth/dev_auth_token_service_noop_test.dart"
      provides: "Test stub login/logout no-op (RED)"
    - path: "test/app/app_wiring_test.dart"
      provides: "Widget test stub RepositoryProvider (RED)"
    - path: "test/network/auth_interceptor_test.dart"
      provides: "Unit test stub dio AuthInterceptor (RED)"
    - path: "test/network/graphql_auth_link_test.dart"
      provides: "Unit test stub graphql AuthLink (RED)"
  key_links:
    - from: "pubspec.yaml"
      to: "flutter_dotenv package"
      via: "dependencies section"
      pattern: "flutter_dotenv:\\s*\\^6"
    - from: "pubspec.yaml flutter.assets"
      to: ".env file"
      via: "asset declaration"
      pattern: "- \\.env"
---

<objective>
Wave 0 di Phase 1: installare le dipendenze mancanti (`flutter_dotenv`, `bloc_test`, `mocktail`), configurare gli asset, creare `.env.example` + `.env` + `.gitignore` entry, e scaffoldare tutti i test RED per DEV-AUTH-01..05 in modalità TDD.

Purpose: I plan downstream (02, 03) implementano contratto, stub e wiring TDD-style. Senza test stub pre-esistenti non esiste RED → GREEN. Questa wave sblocca il ritmo TDD del progetto (regola `testing.md`).

Output: Ambiente pronto per TDD, file test che falliscono per assenza di implementazione (non per errori di compilazione di dipendenze), `.env` dev-user preconfigurato.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dev-auth-stub/01-CONTEXT.md
@.planning/phases/01-dev-auth-stub/01-RESEARCH.md
@.planning/phases/01-dev-auth-stub/01-VALIDATION.md
@.planning/phases/11-auth-session-bootstrap/11-CONTEXT.md
@docs/rules/testing.md
@docs/rules/workflow.md
@pubspec.yaml
@lib/models/user.dart
@lib/models/enums/role_type.dart

<interfaces>
<!-- Tipi esistenti che executor DEVE usare -->

From lib/models/user.dart:
```dart
class User extends Equatable {
  const User({
    required this.id,
    required this.twitchId,
    required this.twitchPoints,
    required this.currentCharacter,
    required this.role,
  });
  final String id;
  final String? twitchId;
  final RoleType? role;
  final int? twitchPoints;
  final Character? currentCharacter;
}
```

From lib/models/enums/role_type.dart:
```dart
enum RoleType { guard, adventurer, innkeeper }
```

Contratto canonico da implementare (Plan 02) — source: 11-CONTEXT.md D-02:
- `Stream<AuthState> get authStateStream`
- `Future<String?> getAccessToken()`
- `Future<void> login()`
- `Future<void> logout()`
- `Future<void> handleRevocation()`

Sealed `AuthState` con sottotipi: `AuthBootstrapping`, `AuthAuthenticated({required User user, required String accessToken})`, `AuthUnauthenticated`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install deps + configure .env assets + gitignore</name>
  <read_first>
    - pubspec.yaml
    - .gitignore (se esiste, altrimenti creare)
    - .planning/phases/01-dev-auth-stub/01-RESEARCH.md §"Standard Stack" + §"Assumptions Log A4"
  </read_first>
  <files>pubspec.yaml, .env.example, .env, .gitignore</files>
  <action>
1. Eseguire: `flutter pub add flutter_dotenv` (runtime dep) — deve risultare `flutter_dotenv: ^6.0.0` o compatibile.
2. Eseguire: `flutter pub add --dev bloc_test mocktail` — devono risultare `bloc_test: ^10.0.0` e `mocktail: ^1.0.5`.
3. Modificare `pubspec.yaml` sezione `flutter.assets`: aggiungere riga `- .env` tra gli asset (pitfall A4 di RESEARCH.md — senza questo `dotenv.load` fallisce). Mantenere gli asset esistenti.
4. Creare `.env.example` (committato in VCS) con contenuto:
   ```
   # Phase 1 Dev Auth Stub — NON contiene secret reali. Copia in .env e riempi.
   DEV_AUTH_ENABLED=true
   DEV_AUTH_ACCESS_TOKEN=replace-with-dev-twitch-access-token
   DEV_AUTH_USER_ID=replace-with-backend-user-id
   DEV_AUTH_TWITCH_ID=replace-with-twitch-channel-id
   DEV_AUTH_ROLE=adventurer
   ```
5. Creare `.env` reale (NON committato) con valori placeholder sviluppo:
   ```
   DEV_AUTH_ENABLED=true
   DEV_AUTH_ACCESS_TOKEN=dev-stub-token-replace-me
   DEV_AUTH_USER_ID=dev-user-id
   DEV_AUTH_TWITCH_ID=dev-twitch-id
   DEV_AUTH_ROLE=adventurer
   ```
6. Modificare `.gitignore`: se non esiste crearlo; aggiungere (se non già presenti):
   ```
   .env
   .env.local
   !.env.example
   ```
7. Eseguire `flutter pub get` per consolidare il lock file.

Implementa D-02, D-03, D-04, D-05 (CONTEXT.md). Niente secret in repo (CLAUDE.md §5).
  </action>
  <verify>
    <automated>flutter pub get &amp;&amp; grep -q "flutter_dotenv" pubspec.yaml &amp;&amp; grep -q "bloc_test" pubspec.yaml &amp;&amp; grep -q "mocktail" pubspec.yaml &amp;&amp; grep -E "^\s*-\s*\.env\s*$" pubspec.yaml &amp;&amp; test -f .env.example &amp;&amp; test -f .env &amp;&amp; grep -q "^\.env$" .gitignore &amp;&amp; git check-ignore .env</automated>
  </verify>
  <acceptance_criteria>
    - `flutter pub get` exit code 0
    - `pubspec.yaml` contiene `flutter_dotenv: ^6.x`, `bloc_test: ^10.x`, `mocktail: ^1.x`
    - `pubspec.yaml` contiene `- .env` sotto `flutter.assets`
    - `.env.example` esiste e documenta le 5 variabili DEV_AUTH_*
    - `.env` esiste ed è ignorato da git (`git check-ignore .env` → exit 0)
    - `.gitignore` contiene la riga `.env`
  </acceptance_criteria>
  <done>Dipendenze installate, asset `.env` registrato, `.env` gitignored, `.env.example` committable.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Scaffold test fixtures + helpers</name>
  <read_first>
    - docs/rules/testing.md §"Mocking" + §"Struttura"
    - .planning/phases/01-dev-auth-stub/01-RESEARCH.md §"Wave 0 Gaps"
    - lib/models/user.dart
    - lib/models/enums/role_type.dart
  </read_first>
  <files>test/helpers/auth_fixtures.dart, test/helpers/fixtures/dev_auth_env.dart</files>
  <action>
1. Creare `test/helpers/auth_fixtures.dart` con fixtures condivise (PascalCase costanti in lowerCamelCase Dart):
   ```dart
   import 'package:klimmeck_guide/models/enums/role_type.dart';
   import 'package:klimmeck_guide/models/user.dart';

   const String testAccessToken = 'dev-stub-token-test';
   const String testUserId = 'user-test-id';
   const String testTwitchId = 'twitch-test-id';

   User buildTestUser({RoleType role = RoleType.adventurer}) => User(
         id: testUserId,
         twitchId: testTwitchId,
         twitchPoints: 0,
         currentCharacter: null,
         role: role,
       );
   ```

2. Creare `test/helpers/fixtures/dev_auth_env.dart` con helper che inizializza `flutter_dotenv` da stringa in-memory (evita dipendenza da file `.env` reale nei test):
   ```dart
   import 'package:flutter_dotenv/flutter_dotenv.dart';

   /// Inizializza dotenv con valori di test. Chiamare in `setUp()`.
   /// Ogni chiave è override-abile via `overrides`.
   Future<void> loadTestEnv({
     String devAuthEnabled = 'true',
     String accessToken = 'dev-stub-token-test',
     String userId = 'user-test-id',
     String twitchId = 'twitch-test-id',
     String role = 'adventurer',
   }) async {
     dotenv.testLoad(fileInput: '''
   DEV_AUTH_ENABLED=$devAuthEnabled
   DEV_AUTH_ACCESS_TOKEN=$accessToken
   DEV_AUTH_USER_ID=$userId
   DEV_AUTH_TWITCH_ID=$twitchId
   DEV_AUTH_ROLE=$role
   ''');
   }
   ```
   Nota: `dotenv.testLoad(fileInput: ...)` è l'API ufficiale di `flutter_dotenv` 6.x per inizializzazione sincrona da stringa. Se la versione installata espone un nome diverso, documentare e usare quello.

3. Nessun accesso a `BuildContext`, niente dipendenze Flutter dei fixtures (regola `architecture.md`: helper puri).
  </action>
  <verify>
    <automated>test -f test/helpers/auth_fixtures.dart &amp;&amp; test -f test/helpers/fixtures/dev_auth_env.dart &amp;&amp; grep -q "buildTestUser" test/helpers/auth_fixtures.dart &amp;&amp; grep -q "loadTestEnv" test/helpers/fixtures/dev_auth_env.dart &amp;&amp; dart analyze test/helpers/</automated>
  </verify>
  <acceptance_criteria>
    - `test/helpers/auth_fixtures.dart` esiste, esporta `testAccessToken`, `testUserId`, `testTwitchId`, `buildTestUser()`
    - `test/helpers/fixtures/dev_auth_env.dart` esiste, esporta `loadTestEnv()` async function
    - `dart analyze test/helpers/` è pulito (no errors, no warnings)
  </acceptance_criteria>
  <done>Fixture e helper env-test disponibili per tutti i test downstream di Plan 02 e Plan 03.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Scaffold RED test stubs per DEV-AUTH-01..05</name>
  <read_first>
    - docs/rules/testing.md
    - .planning/phases/01-dev-auth-stub/01-VALIDATION.md §"Per-Task Verification Map"
    - .planning/phases/01-dev-auth-stub/01-RESEARCH.md §"Phase Requirements → Test Map"
    - test/helpers/auth_fixtures.dart (appena creato)
    - test/helpers/fixtures/dev_auth_env.dart (appena creato)
  </read_first>
  <files>
    test/repository/services/auth/auth_token_service_contract_test.dart,
    test/repository/services/auth/dev_auth_token_service_test.dart,
    test/repository/services/auth/dev_auth_token_service_role_test.dart,
    test/repository/services/auth/dev_auth_token_service_noop_test.dart,
    test/app/app_wiring_test.dart,
    test/network/auth_interceptor_test.dart,
    test/network/graphql_auth_link_test.dart
  </files>
  <action>
Creare 7 test file che esprimono il comportamento atteso PRIMA dell'implementazione (RED state — il pattern TDD del progetto richiede che la classe sotto test NON esista ancora, quindi i test falliranno per `import` non risolti: questo è lo stato RED atteso della Wave 0).

Ogni file DEVE avere un `main()` con almeno un `test()` o `testWidgets()` concreto. Usare `TODO(phase-1):` marker solo dove un dettaglio specifico richiede resolve in Plan 02/03 — non usare `TODO` come sostituto del test.

**File 1 — `test/repository/services/auth/auth_token_service_contract_test.dart` (DEV-AUTH-01):**
Import atteso: `package:klimmeck_guide/repository/services/auth/auth_token_service.dart`.
Test che verificano:
- `DevAuthTokenService()` è assegnabile a variabile `AuthTokenService` (contratto rispettato).
- `AuthState` ha 3 sottotipi: `AuthBootstrapping`, `AuthAuthenticated`, `AuthUnauthenticated`.
- `AuthAuthenticated` espone campi `user` (User) e `accessToken` (String).
- I 5 metodi esistono con signature esatta: `authStateStream`, `getAccessToken()`, `login()`, `logout()`, `handleRevocation()`.

**File 2 — `test/repository/services/auth/dev_auth_token_service_test.dart` (DEV-AUTH-02):**
Import atteso: `package:klimmeck_guide/repository/services/auth/dev_auth_token_service.dart` + `auth_token_service.dart`.
In `setUp()` chiamare `await loadTestEnv();`.
Test:
- `blocTest`-style tramite `expectLater` sullo stream: `initialize()` emette `AuthBootstrapping` seguito da `AuthAuthenticated` con `user.id == testUserId`, `user.twitchId == testTwitchId`, `accessToken == testAccessToken`.
- `getAccessToken()` dopo init ritorna `testAccessToken`.

**File 3 — `test/repository/services/auth/dev_auth_token_service_role_test.dart` (DEV-AUTH-03):**
Test parametrico per ogni role ∈ `{guard, adventurer, innkeeper}`:
- `loadTestEnv(role: 'innkeeper')` → dopo `initialize()` stream emette `AuthAuthenticated` con `user.role == RoleType.innkeeper`. Idem per `guard` e `adventurer`.
- Test edge case: `loadTestEnv(role: 'UnknownRole')` → stub fallback a `RoleType.adventurer` (pitfall 4 in RESEARCH.md: normalizzazione + fallback).

**File 4 — `test/repository/services/auth/dev_auth_token_service_noop_test.dart` (DEV-AUTH-04):**
Test:
- `login()` completa senza lanciare e NON emette nuovi AuthState sullo stream.
- `logout()` completa senza lanciare e NON emette nuovi AuthState.
- `handleRevocation()` completa senza lanciare e NON emette.
- Usare un `StreamController.listen` con `expectAsync1` che non verrebbe chiamato — o `emitsInOrder([... nothing ...])` con timeout breve.

**File 5 — `test/app/app_wiring_test.dart` (DEV-AUTH-05):**
Widget test: montare un widget tree minimale che wrappi `RepositoryProvider<AuthTokenService>` con una fake `DevAuthTokenService` (mocktail), dentro un `MultiBlocProvider` vuoto, verificare che `context.read<AuthTokenService>()` ritorni l'istanza senza lanciare `ProviderNotFoundException` (pitfall 3 RESEARCH.md).

**File 6 — `test/network/auth_interceptor_test.dart` (DEV-AUTH-05):**
Import atteso: `package:klimmeck_guide/repository/services/rest/auth_interceptor.dart` (sarà creato in Plan 03).
Test: data un `MockAuthTokenService` che ritorna `'token-abc'` da `getAccessToken()`, l'interceptor aggiunge l'header `Authorization: Bearer token-abc` alla `RequestOptions`. Se `getAccessToken()` ritorna `null`, l'header NON viene aggiunto.

**File 7 — `test/network/graphql_auth_link_test.dart` (DEV-AUTH-05):**
Import atteso: `package:klimmeck_guide/repository/services/graphql/auth_link.dart` (sarà creato in Plan 03).
Test: data una fake `AuthTokenService` che ritorna `'token-xyz'`, una `Request` GraphQL passata attraverso l'`AuthLink` ha `context.entry<HttpLinkHeaders>().headers['Authorization'] == 'Bearer token-xyz'`.

Tutti i file devono:
- Avere `void main()` con body non vuoto.
- Importare `package:flutter_test/flutter_test.dart`.
- Essere sintatticamente validi (il fallimento deve avvenire a livello di import di classi non ancora esistenti, non a livello di parsing).
  </action>
  <verify>
    <automated>test -f test/repository/services/auth/auth_token_service_contract_test.dart &amp;&amp; test -f test/repository/services/auth/dev_auth_token_service_test.dart &amp;&amp; test -f test/repository/services/auth/dev_auth_token_service_role_test.dart &amp;&amp; test -f test/repository/services/auth/dev_auth_token_service_noop_test.dart &amp;&amp; test -f test/app/app_wiring_test.dart &amp;&amp; test -f test/network/auth_interceptor_test.dart &amp;&amp; test -f test/network/graphql_auth_link_test.dart &amp;&amp; ! flutter test test/repository/services/auth/ test/app/ test/network/ 2>&amp;1 | tee /tmp/phase1_wave0_red.log &amp;&amp; grep -qE "(compilation|Target of URI doesn't exist|Undefined (class|name))" /tmp/phase1_wave0_red.log</automated>
  </verify>
  <acceptance_criteria>
    - 7 file test esistono ai percorsi indicati
    - Ogni file ha `void main()` con almeno un `test(...)` o `testWidgets(...)` non vuoto
    - `flutter test` sui tre path (`test/repository/services/auth/`, `test/app/`, `test/network/`) fallisce con errori di import/"Undefined class" — conferma lo stato RED atteso per TDD (sarà GREEN dopo Plan 02/03)
    - Nessun TODO sostituisce un test — ogni `test()` ha un `expect(...)` concreto
  </acceptance_criteria>
  <done>Test RED committati. Plan 02 e 03 li porteranno a GREEN implementando contratto, stub e wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → app | File `.env` letto da `flutter_dotenv` al runtime; untrusted contenuto teoricamente modificabile dall'utente sviluppatore |
| VCS → developer workstation | `.env.example` committato, `.env` NO; rischio di leak credentials se gitignore sbagliato |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Information Disclosure | `.env` file con token di dev | mitigate | `.gitignore` contiene `.env`; test `git check-ignore .env` exit 0; `.env.example` contiene solo placeholder; CLAUDE.md §5 rinforza la regola |
| T-01-01-02 | Tampering | `.env` modificato localmente (es. ruolo `innkeeper` forzato) | accept | Phase 1 è dev-only; il backend enforce role su mutation (regola CLAUDE.md §4 "Backend fonte di verità") indipendentemente dal client |
| T-01-01-03 | Information Disclosure | `.env` incluso accidentalmente in release build | mitigate | Plan 03 Task 3 aggiungerà guardia `DEV_AUTH_ENABLED` runtime check + security note in SUMMARY.md per verificare che `.env` sia rimosso dagli assets in build release futuri (Phase 11/12) |
| T-01-01-04 | Spoofing | Test scrivono su `.env` reale invece di env in-memory | mitigate | `loadTestEnv()` usa `dotenv.testLoad(fileInput:)` in-memory, i test non toccano mai il file `.env` on disk |
</threat_model>

<verification>
- `flutter pub get` exit code 0
- `flutter analyze` pulito su `test/helpers/` e `lib/` (nessuna nuova warning introdotta)
- `flutter test` fallisce nei test stub per errori di import su classi/file non ancora creati (stato RED TDD voluto)
- `git check-ignore .env` exit code 0
- `git status` mostra `.env.example`, `.gitignore`, `pubspec.yaml`, `pubspec.lock`, `test/helpers/**`, `test/repository/**`, `test/app/**`, `test/network/**` come file modificati/nuovi — NON mostra `.env`
</verification>

<success_criteria>
- Tutte le dipendenze (`flutter_dotenv`, `bloc_test`, `mocktail`) installate nel range corretto
- `.env` asset registrato in `pubspec.yaml`
- `.env` creato ma gitignored
- `.env.example` committable e documentato
- 7 test stub RED committati, pronti per TDD in Plan 02/03
- 2 helper di test (fixtures + env loader) funzionanti e analizzati puliti
</success_criteria>

<output>
After completion, create `.planning/phases/01-dev-auth-stub/01-01-SUMMARY.md` con:
- Elenco dipendenze aggiunte con versioni effettive
- Path dei 7 test stub creati
- Conferma `.env` gitignored
- Nota per Plan 02/03 sullo stato RED atteso
</output>
