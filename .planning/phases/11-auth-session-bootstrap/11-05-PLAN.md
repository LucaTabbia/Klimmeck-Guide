---
phase: 11-auth-session-bootstrap
plan: 05
type: execute
wave: 5
depends_on: ["01-03", "01-04"]
files_modified:
  - lib/main.dart
  - lib/routes/routes.dart
  - lib/screens/splash/splash_screen.dart
  - lib/screens/splash/cubit/splash_cubit.dart
  - lib/screens/splash/cubit/splash_state.dart
  - lib/screens/signIn/sign_in_screen.dart
  - lib/screens/auth/auth_gate.dart
  - test/screens/splash/cubit/splash_cubit_test.dart
  - test/screens/signIn/sign_in_screen_test.dart
  - test/screens/auth/auth_gate_test.dart
  - .planning/phases/11-auth-session-bootstrap/BACKEND-NOTES.md
autonomous: false
requirements: [AUTH-01, AUTH-03, AUTH-04, AUTH-07]
user_setup:
  - service: twitch
    why: "Verifica deep link + flusso OAuth end-to-end"
    env_vars:
      - name: TWITCH_CLIENT_ID
        source: "Twitch Developer Console"
    dashboard_config:
      - task: "Verificare redirect URI `klimmeck://auth` attiva"
        location: "Twitch Developer Console → App settings"
must_haves:
  truths:
    - "Al cold start l'app entra in Splash; con token valido in SecureStorage + validate 200 → entra direttamente nel main shell (nessun prompt)"
    - "Al cold start senza token → routing immediato a SignInScreen (nessun delay)"
    - "Al cold start con token non più valido → messaggio 'La sessione è scaduta, accedi di nuovo.' poi SignInScreen"
    - "Dopo 10s di bootstrap instabile, lo Splash mostra il messaggio non-bloccante + pulsante 'Accedi manualmente' che naviga a SignInScreen; retry continua in background"
    - "Tap su 'Login con Twitch' apre system browser; al ritorno OAuth l'utente è autenticato e vede il main shell"
    - "L'annullamento del browser OAuth (D-21) lascia la SignInScreen invariata, nessun error banner"
    - "MultiBlocProvider di feature (tutti i Cubit gameplay pre-esistenti) sta DENTRO il subtree `AuthenticatedShell`, non al root; al logout il subtree viene smontato e i Cubit sono disposti"
  artifacts:
    - path: "lib/main.dart"
      provides: "Entry con RepositoryProvider(AuthTokenService, GraphqlClientProvider) + AuthCubit root + AuthGate"
    - path: "lib/screens/auth/auth_gate.dart"
      provides: "Widget che osserva AuthCubit e monta Splash / SignIn / AuthenticatedShell"
      exports: ["AuthGate"]
    - path: "lib/screens/splash/splash_screen.dart"
      provides: "Splash screen immersiva conforme a 11-UI-SPEC.md con stati auth-resolve"
    - path: "lib/screens/splash/cubit/splash_cubit.dart"
      provides: "Cubit esteso per cold-start gate (D-17, D-18)"
    - path: "lib/screens/signIn/sign_in_screen.dart"
      provides: "SignIn utility screen conforme a 11-UI-SPEC.md"
    - path: ".planning/phases/11-auth-session-bootstrap/BACKEND-NOTES.md"
      provides: "Handoff BE: connection_init contract, scope OAuth, endpoint FE consuma"
  key_links:
    - from: "lib/main.dart"
      to: "AuthTokenService.onLogoutCompleted"
      via: "graphqlClientProvider.recreate (wired at construction)"
      pattern: "onLogoutCompleted:.*recreate"
    - from: "lib/screens/auth/auth_gate.dart"
      to: "AuthCubitState"
      via: "BlocBuilder"
      pattern: "BlocBuilder<AuthCubit, AuthCubitState>"
    - from: "lib/screens/splash/cubit/splash_cubit.dart"
      to: "AuthTokenService.bootstrap"
      via: "cubit method"
      pattern: "authService.bootstrap"
---

<objective>
Chiudere la fase cablando tutti i pezzi in `main.dart`, costruendo l'`AuthGate` che routing su AuthCubit state, estendendo `SplashCubit`/`SplashScreen` con gli stati cold-start (D-17, D-18, D-10, D-19), implementando la `SignInScreen` conforme a 11-UI-SPEC.md, spostando i Cubit di feature dentro il subtree autenticato (pitfall #7), e producendo `BACKEND-NOTES.md` per l'handoff BE.

Purpose: È il plan "di integrazione". Ha 1 checkpoint umano (verifica visiva + flusso OAuth su device reale) perché AUTH-01 richiede un vero browser di sistema + deep link, cosa non automatizzabile (vedi 11-VALIDATION.md §Manual-Only Verifications).
Output: app compilabile e avviabile, flusso OAuth end-to-end funzionante, UI conforme al contratto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-auth-session-bootstrap/11-CONTEXT.md
@.planning/phases/11-auth-session-bootstrap/11-UI-SPEC.md
@.planning/phases/11-auth-session-bootstrap/11-VALIDATION.md
@.planning/phases/01-02-SUMMARY.md
@.planning/phases/01-03-SUMMARY.md
@.planning/phases/01-04-SUMMARY.md
@docs/rules/ui-ux.md
@docs/rules/architecture.md
@lib/main.dart
@lib/routes/routes.dart
@lib/screens/splash/splash_screen.dart
@lib/screens/splash/cubit/splash_cubit.dart
@lib/screens/splash/cubit/splash_state.dart
@lib/screens/signIn/sign_in_screen.dart
@lib/theme/kg_theme.dart

<interfaces>
```dart
// lib/screens/auth/auth_gate.dart
class AuthGate extends StatelessWidget {
  final Widget authenticatedShell;    // existing MainScreen + its MultiBlocProvider subtree
  final Widget signInScreen;          // SignInScreen
  final Widget splash;                // SplashScreen
  const AuthGate({super.key, required this.authenticatedShell, required this.signInScreen, required this.splash});
}

// lib/screens/splash/cubit/splash_state.dart
sealed class SplashState extends Equatable { const SplashState(); }
class SplashBootstrapping extends SplashState { const SplashBootstrapping(); @override List<Object?> get props => []; }
class SplashNetworkDelayed extends SplashState { const SplashNetworkDelayed(); @override List<Object?> get props => []; }
class SplashSessionExpired extends SplashState { const SplashSessionExpired(); @override List<Object?> get props => []; }
class SplashAuthenticated extends SplashState { const SplashAuthenticated(); @override List<Object?> get props => []; }
class SplashUnauthenticated extends SplashState { const SplashUnauthenticated(); @override List<Object?> get props => []; }

// lib/screens/splash/cubit/splash_cubit.dart
class SplashCubit extends Cubit<SplashState> {
  SplashCubit({required AuthTokenService authService, Duration instabilityThreshold = const Duration(seconds: 10)});
  Future<void> start();          // kicks off bootstrap + 10s instability timer
  void manualSignInRequested();  // user tapped "Accedi manualmente" button
  @override Future<void> close();
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SplashCubit esteso + SignInScreen + AuthGate</name>
  <read_first>
    - lib/screens/splash/splash_screen.dart (intero)
    - lib/screens/splash/cubit/splash_cubit.dart (intero)
    - lib/screens/splash/cubit/splash_state.dart (intero)
    - lib/screens/signIn/sign_in_screen.dart (stub esistente)
    - .planning/phases/11-auth-session-bootstrap/11-UI-SPEC.md §SplashScreen, §SignInScreen (intere)
    - lib/theme/kg_theme.dart
    - test/screens/splash/cubit/splash_cubit_test.dart (skeleton plan 01)
    - test/screens/signIn/cubit/sign_in_cubit_test.dart (già PASS da plan 04)
  </read_first>
  <behavior>
    **SplashCubit** (estensione del cubit esistente che fa SVG preload):
    - `start()`:
      - `emit(SplashBootstrapping())`
      - Avvia timer 10s → se scade e stato è ancora `Bootstrapping` → `emit(SplashNetworkDelayed())` ma continua retry.
      - Esegue `await authService.bootstrap()` in try/retry loop (backoff 2s, 4s, 8s, max 30s) finché:
        - `authService.currentState is AuthAuthenticated` → cancella timer → `emit(SplashAuthenticated())`
        - `authService.currentState is AuthUnauthenticated(reason: initial)` → `emit(SplashUnauthenticated())` (D-19 instant routing)
        - `authService.currentState is AuthUnauthenticated(reason: sessionExpired)` → `emit(SplashSessionExpired())` → dopo 1500ms `emit(SplashUnauthenticated())` (UI-SPEC §SplashSessionExpired)
        - Errore transitorio → continua retry
    - `manualSignInRequested()`: cancella timer + retry loop → `emit(SplashUnauthenticated())`.
    - `close()`: cancella timer.

    **SplashScreen** (estensione widget esistente):
    - Mantiene `_AnimatedLoadingText` e `splash.png` full-bleed. Bottom padding del testo: **48px** (era 50 — correggere come richiesto da 11-UI-SPEC §Spacing, Boy Scout Rule).
    - `BlocBuilder<SplashCubit, SplashState>` renderizza:
      - `SplashBootstrapping` → `_AnimatedLoadingText` con testo "Caricamento" + dots animate.
      - `SplashNetworkDelayed` → testo "Connessione a Twitch instabile, attendere o accedere manualmente" in `specialText` + TextButton "Accedi manualmente" che chiama `context.read<SplashCubit>().manualSignInRequested()`.
      - `SplashSessionExpired` → testo "La sessione è scaduta, accedi di nuovo." in `specialText` (ephemeral, poi routed).
      - `SplashAuthenticated` / `SplashUnauthenticated` → nessuna UI extra (routing è compito di `AuthGate`).
    - NO `Scaffold` chrome, NO `AppBar`.

    **SignInScreen** (costruire da stub):
    - `Scaffold(appBar: AppBar(/*default theme: darkBronze bg, primaryGold fg*/), body: DecoratedBox(decoration: KlimmeckGuideTheme.instance.getBackgroundDecoration(), child: ...))`
    - Layout (top → bottom, padding md):
      1. Spacer top (lg)
      2. Klimmeck logo SVG (60% screen width) centered
      3. Gap 2xl (48px)
      4. Tagline text (`bodyLarge` Cormorant Garamond) — copy placeholder "Il tuo tempo sullo stream è progressione." (accettabile default; non specificato in UI-SPEC quindi scelta d'istinto documentata)
      5. Gap xl (32px)
      6. `ElevatedButton` "Login con Twitch" con gradient `primaryGold → darkBronze`, border radius 15, min height 44, icon Twitch SVG 24px sinistra, label `titleMedium` deepNight. On state `SignInInProgress` → replace contenuto con `CircularProgressIndicator(strokeWidth: 2, color: deepNight)` 20×20. `onPressed: context.read<SignInCubit>().login()`.
      7. Gap md (16px)
      8. `BlocBuilder<SignInCubit, SignInState>` — se `SignInError(msg)` → `Text(msg, style: KlimmeckGuideTheme.errorText)`. Altrimenti `SizedBox.shrink()`.
      9. Spacer
      10. Footer row: TextButton "Termini di servizio" + "Privacy" (URL placeholder `#`), style `lightText`.
    - Se `AuthCubit` state è `AuthUnauthenticatedShell(reason: sessionExpired)` → al primo frame mostra uno SnackBar con "La sessione è scaduta, accedi di nuovo." (hook via `BlocListener<AuthCubit, AuthCubitState>` in `initState`).

    **AuthGate**:
    - `BlocBuilder<AuthCubit, AuthCubitState>` builder:
      - `AuthInitializing` → `splash`
      - `AuthenticatedShell` → `authenticatedShell` (Plan 05 passerà il widget esistente `MainScreen` dentro al suo `MultiBlocProvider` di feature)
      - `AuthLoggingOut` → `splash` (o un widget placeholder minimale; scelta: reuse splash)
      - `AuthUnauthenticatedShell` → `signInScreen`

    **Tests obbligatori** (rimuovere `skip` dagli skeleton dove applicabile):
    - SplashCubit bloc_test (usare fakeAsync per il timer):
      - `emits [Bootstrapping, Authenticated] when authService bootstrap returns Authenticated` (AUTH-03)
      - `emits [Bootstrapping, Unauthenticated] when no refresh token` (AUTH-03)
      - `emits [Bootstrapping, SessionExpired, Unauthenticated] on reason=sessionExpired` (AUTH-07)
      - `emits [Bootstrapping, NetworkDelayed] after 10s without resolution, keeps retrying`
      - `manualSignInRequested emits Unauthenticated even during retry loop`
    - SignInScreen widget_test:
      - smoke render con `SignInIdle`
      - button tap → `SignInCubit.login` called
      - `SignInInProgress` state → button shows spinner, no label
      - `SignInError('msg')` → inline Text with `errorText` style is visible
      - AUTH-07: `AuthCubit` emits `AuthUnauthenticatedShell(sessionExpired)` → SnackBar "La sessione è scaduta, accedi di nuovo." is shown
    - AuthGate widget_test:
      - `AuthInitializing` → Splash visible
      - `AuthenticatedShell` → authenticated child visible, Splash/SignIn NOT in tree
      - `AuthUnauthenticatedShell` → SignIn visible
      - transition Authenticated → Unauthenticated tears down child subtree (use a widget that registers its dispose in a flag)
  </behavior>
  <files>
    lib/screens/splash/cubit/splash_state.dart,
    lib/screens/splash/cubit/splash_cubit.dart,
    lib/screens/splash/splash_screen.dart,
    lib/screens/signIn/sign_in_screen.dart,
    lib/screens/auth/auth_gate.dart,
    test/screens/splash/cubit/splash_cubit_test.dart,
    test/screens/signIn/sign_in_screen_test.dart,
    test/screens/auth/auth_gate_test.dart
  </files>
  <action>
    TDD RED → GREEN per ciascun file:
    1. Riempire i test esistenti, crearne dove mancano. Commit: `test(auth): RED for SplashCubit extensions, SignInScreen, AuthGate`.
    2. Implementare:
       - `splash_state.dart` con sealed classes
       - `splash_cubit.dart` con `start()` + retry loop + timer (evitare `Future.delayed` magici nei test: usare `Duration` injection via costruttore, oppure `Stopwatch` + Timer con clock iniettabile).
       - Estendere `splash_screen.dart` con `BlocBuilder`, correggere bottom padding 50→48.
       - Costruire `sign_in_screen.dart` completo (usa `BlocProvider.of<SignInCubit>` con cubit iniettato dal parent in plan task 2).
       - Creare `auth_gate.dart`.
    3. Commit: `feat(auth): wire splash + sign-in + auth gate to AuthCubit/SignInCubit`.
    4. **REFACTOR** opzionale: estrarre il Twitch button in `lib/screens/signIn/components/twitch_login_button.dart` se il widget è >60 righe inline.

    Copy strings ESATTE (no variazioni):
    - "Caricamento" — esistente, verificare
    - "Connessione a Twitch instabile, attendere o accedere manualmente"
    - "Accedi manualmente"
    - "La sessione è scaduta, accedi di nuovo."
    - "Login con Twitch"
    - "Errore di connessione, riprova"
    - "Termini di servizio"
    - "Privacy"
  </action>
  <verify>
    <automated>flutter test test/screens/splash/ test/screens/signIn/ test/screens/auth/ && flutter analyze</automated>
  </verify>
  <acceptance_criteria>
    - Tutti i test nei 3 path PASS 0 skip
    - `grep -q "SplashNetworkDelayed" lib/screens/splash/cubit/splash_state.dart`
    - `grep -q "SplashSessionExpired" lib/screens/splash/cubit/splash_state.dart`
    - `grep -q "Login con Twitch" lib/screens/signIn/sign_in_screen.dart`
    - `grep -q "La sessione è scaduta, accedi di nuovo." lib/screens/signIn/sign_in_screen.dart` OR `lib/screens/splash/splash_screen.dart`
    - `grep -q "Accedi manualmente" lib/screens/splash/splash_screen.dart`
    - `grep -q "class AuthGate" lib/screens/auth/auth_gate.dart`
    - `grep -q "BlocBuilder<AuthCubit" lib/screens/auth/auth_gate.dart`
    - `grep -n "50" lib/screens/splash/splash_screen.dart` → 0 match come bottom padding (deve essere 48)
    - `flutter analyze` exit code 0
  </acceptance_criteria>
  <done>Splash + SignIn + AuthGate testati e verdi; UI conforme a 11-UI-SPEC.md; requisiti AUTH-01 (UI), AUTH-03 (cold-start), AUTH-07 (UX messaggi) coperti a livello widget.</done>
</task>

<task type="auto">
  <name>Task 2: main.dart restructure + routes + BACKEND-NOTES</name>
  <read_first>
    - lib/main.dart (intero)
    - lib/routes/routes.dart (intero)
    - .planning/phases/11-auth-session-bootstrap/11-RESEARCH.md §Pitfall 7 (MultiBlocProvider root non ristrutturato)
    - .planning/phases/01-02-SUMMARY.md
    - .planning/phases/01-03-SUMMARY.md
    - .planning/phases/01-04-SUMMARY.md
    - CLAUDE.md §Principi trasversali
  </read_first>
  <files>
    lib/main.dart,
    lib/routes/routes.dart,
    .planning/phases/11-auth-session-bootstrap/BACKEND-NOTES.md
  </files>
  <action>
    1. `lib/main.dart` — ristrutturazione:
       - `WidgetsFlutterBinding.ensureInitialized()`
       - Istanziare le dipendenze auth (lazy ok ma preferibile eager):
         ```dart
         final secureStorage = SecureStorageService(const FlutterSecureStorage());
         final twitchDio = Dio(); // bare per Twitch endpoints
         final twitchApi = TwitchAuthApi(twitchDio);
         final pkce = PkceGenerator();

         // Graphql provider creato lazy ma prima di AuthTokenService (serve come receiver del callback)
         late final AuthTokenService authService;
         late final GraphqlClientProvider graphqlProvider;

         authService = AuthTokenService(
           storage: secureStorage,
           api: twitchApi,
           pkce: pkce,
           webAuthenticator: FlutterWebAuth2.authenticate,
           onLogoutCompleted: () => graphqlProvider.recreate(),
         );
         graphqlProvider = GraphqlClientProvider(authService: authService);
         ```
       - `runApp` con albero:
         ```dart
         MultiRepositoryProvider(
           providers: [
             RepositoryProvider<AuthTokenService>.value(value: authService),
             RepositoryProvider<GraphqlClientProvider>.value(value: graphqlProvider),
             RepositoryProvider<AuthenticatedDio>(create: (_) => AuthenticatedDioFactory.create(authService: authService, baseUrl: EnvConfig.restBaseUrl)),
           ],
           child: BlocProvider(
             create: (ctx) => AuthCubit(authService: authService),
             child: MaterialApp(
               theme: ...,
               routes: appRoutes,
               home: GraphQLProvider(
                 client: graphqlProvider.clientNotifier,
                 child: AuthGate(
                   splash: BlocProvider(create: (_) => SplashCubit(authService: authService)..start(), child: const SplashScreen()),
                   signInScreen: BlocProvider(create: (_) => SignInCubit(authService: authService), child: const SignInScreen()),
                   authenticatedShell: const MainScreen(), // il suo MultiBlocProvider interno resta com'è
                 ),
               ),
             ),
           ),
         )
         ```
       - **Critico (pitfall #7)**: spostare TUTTI i cubit di feature pre-esistenti (CharacterCubit, QuestCubit, MainScreenCubit, ecc. — leggere `main.dart` corrente per l'inventario esatto) DAL root DENTRO `MainScreen` o un `MultiBlocProvider` wrapper immediato del `authenticatedShell`. Solo `AuthCubit` + (eventualmente) `ThemeCubit` non-auth-dipendenti restano al root.
       - NON inizializzare `Firebase.initializeApp()` in questa fase (pitfall #6). Se attualmente presente, rimuoverlo con commento `// Firebase init deferred to Phase 5 (Notifications)`. Se i Cubit di feature falliscono per mancanza Firebase → è atteso, verranno riattivati in Phase 5.

    2. `lib/routes/routes.dart` — verificare che le route `/signin`, `/splash`, `/main` siano registrate OPPURE sostituite dal pattern `AuthGate home=`. Se le route esistenti sono obsolete (uso di `navigatorKey.currentContext!`), rimuoverle. Mantenere solo le route necessarie per `Navigator.pushNamed` interne al main shell.

    3. `.planning/phases/11-auth-session-bootstrap/BACKEND-NOTES.md` — creare il file con le sezioni obbligatorie (convenzione progetto):
       ```markdown
       # Phase 11 — Backend Handoff Notes

       ## Endpoints consumed by FE
       - POST https://id.twitch.tv/oauth2/token (PKCE authorization_code + refresh_token grant)
       - GET  https://id.twitch.tv/oauth2/validate (cold-start + potential in-session checks)
       - POST https://id.twitch.tv/oauth2/revoke (best-effort on logout)
       - (app backend) nessuno in questa fase — auth è solo verso Twitch

       ## GraphQL connection_init contract (OPEN — see 11-RESEARCH.md Open Question #1)
       Requested: il FE invia al WS `connection_init` payload = `{'Authorization': 'Bearer <twitch_access_token>'}`.
       **Azioni BE richieste:**
       - Confermare la chiave atteso (`Authorization` vs `token`).
       - Confermare se dopo token refresh il BE accetta il nuovo token su reconnect automatico o richiede esplicito `connection_terminate` + `connection_init`.
       - Documentare quali scope Twitch sono richiesti per identificare il viewer lato BE.

       ## OAuth scopes (OPEN — Open Question #2)
       Attualmente FE invia `scope=user:read:email`. Da confermare con BE se servono scope aggiuntivi (canale, ecc.).

       ## Topic FCM
       N/A in questa fase (Phase 5 Notifications).

       ## Modelli
       N/A in questa fase — nessun modello di dominio introdotto. `User.twitchId` è già modellato.

       ## Decisioni implementative (FE) che impattano BE
       - D-12: logout client-side dispose+ricrea GraphQLClient. BE non deve mantenere stato per-device-session oltre la WS connection.
       - D-13: revoca token client-side è best-effort. BE può assumere che un token possa rimanere "tecnicamente valido" fino alla scadenza naturale anche se l'utente ha fatto logout.
       - D-08/D-09: revocation detection via `invalid_grant`. BE non è coinvolto (solo Twitch).
       ```

       Se il file esiste già (convenzione "per fase"), aggiornarlo; altrimenti crearlo.
  </action>
  <verify>
    <automated>flutter analyze && flutter test && grep -q "AuthGate" lib/main.dart && grep -q "RepositoryProvider<AuthTokenService>" lib/main.dart && grep -q "onLogoutCompleted" lib/main.dart && test -f .planning/phases/11-auth-session-bootstrap/BACKEND-NOTES.md</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze` exit code 0
    - `flutter test` full suite PASS 0 skip
    - `grep -c "MultiRepositoryProvider" lib/main.dart` ≥ 1 OR `grep -c "RepositoryProvider<AuthTokenService>" lib/main.dart` ≥ 1
    - `grep -q "AuthCubit" lib/main.dart`
    - `grep -q "AuthGate" lib/main.dart`
    - `grep -q "graphqlProvider.recreate" lib/main.dart` OR `grep -q "onLogoutCompleted" lib/main.dart`
    - `grep -n "Firebase.initializeApp" lib/main.dart` → 0 match active (commented-out ok)
    - `grep -n "navigatorKey.currentContext!" lib/` → 0 match anywhere
    - `.planning/phases/11-auth-session-bootstrap/BACKEND-NOTES.md` exists with sections: Endpoints, connection_init contract, OAuth scopes, Topic FCM, Modelli, Decisioni FE
  </acceptance_criteria>
  <done>App compila e avvia; struttura provider corretta; Firebase non è inizializzato; pitfall #7 risolto; BACKEND-NOTES pronte per handoff.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verifica manuale OAuth end-to-end + cold-start resume + external revocation</name>
  <read_first>
    - .planning/phases/11-auth-session-bootstrap/11-VALIDATION.md §Manual-Only Verifications
    - .planning/phases/11-auth-session-bootstrap/11-UI-SPEC.md
  </read_first>
  <what-built>
    Flusso completo di Phase 11: Splash cold-start gate → SignIn utility screen → OAuth PKCE via system browser → callback `klimmeck://auth` → main shell. Logout dialog con teardown. Cold-start resume. Rilevamento revoca esterna.
  </what-built>
  <how-to-verify>
    Esecuzione su device reale (iOS + Android se possibile; Android minimo obbligatorio). Build con `flutter run --dart-define=TWITCH_CLIENT_ID=<id> --dart-define=GRAPHQL_HTTP_URL=<url> --dart-define=GRAPHQL_WS_URL=<wsurl>`.

    **Scenario A — First login (AUTH-01):**
    1. Disinstalla app se già installata (clear keychain).
    2. Apri app → deve apparire Splash → immediatamente SignInScreen (nessun token in storage).
    3. AppBar darkBronze, layout come 11-UI-SPEC.md §SignInScreen (logo, tagline, button "Login con Twitch", footer).
    4. Tap "Login con Twitch" → button mostra spinner → sistema browser si apre con URL `id.twitch.tv/oauth2/authorize?...force_verify=true...`.
    5. Completa login su Twitch.
    6. Browser si chiude → app torna attiva → Splash breve o diretto → main shell.
    Expected: ✅ Autenticato, pronto per Phase 2.

    **Scenario B — Cold-start resume (AUTH-03):**
    1. Dopo Scenario A, fai swipe-kill dell'app.
    2. Riapri app.
    Expected: ✅ Splash → main shell direttamente, zero prompt Twitch, zero browser aperto.

    **Scenario C — Logout (AUTH-04):**
    1. Da main shell, triggera logout (se non c'è ancora un punto UI in questa fase, chiamare `context.read<AuthCubit>().logout(context: context)` via debug button temporaneo — rimosso prima della PR).
    2. Dialog: "Sei sicuro di voler uscire?" → tap "Esci".
    Expected: ✅ Tear-down eseguito, ritorno a SignInScreen, nessun dato visibile della sessione precedente.

    **Scenario D — Switch account (AUTH-05):**
    1. Dopo Scenario C, tap "Login con Twitch" e completa con **account Twitch diverso**.
    Expected: ✅ Main shell mostra identità del secondo account (verificare `userId` diverso da prima). Nessun bleed-through.

    **Scenario E — OAuth cancel (D-21):**
    1. Da SignInScreen tap "Login con Twitch" → browser apre.
    2. Annulla (chiudi browser tab / back gesture).
    Expected: ✅ SignInScreen invariata, nessun banner errore, button di nuovo attivo.

    **Scenario F — External revocation (AUTH-07):**
    1. Mentre loggato, vai su https://www.twitch.tv/settings/connections e revoca manualmente l'app.
    2. Triggera un'azione client che forza un refresh (es. aspetta scadenza proactive refresh, oppure mantieni aperto fino a next proactive tick — documentare timing nel report).
    Expected: ✅ App ritorna a SignInScreen con messaggio "La sessione è scaduta, accedi di nuovo." visibile.

    **Scenario G — Network instability cold-start (D-18):**
    1. Spegni rete del device PRIMA di avviare l'app (da autenticato).
    2. Avvia app → Splash bootstrap.
    3. Dopo ~10s deve apparire "Connessione a Twitch instabile, attendere o accedere manualmente" + bottone "Accedi manualmente".
    4. Riaccendi rete → (ramo 1) splash prosegue automaticamente a main shell, OPPURE (ramo 2) tap "Accedi manualmente" → SignInScreen.
    Expected: ✅ Entrambi i rami osservabili.

    **Report da produrre** (inline nella resume signal):
    - Per ciascuno scenario: ✅/❌ + screenshot/screen recording path se fallisce.
    - Piattaforme testate: iOS X.Y / Android A.B.
    - Eventuali deviazioni dalla UI-SPEC (tipografia, colore, spacing).
  </how-to-verify>
  <acceptance_criteria>
    User signals one of:
    - "approved — all 7 scenarios green on [platforms]"
    - lista di issue specifici (scenario + deviazione) → il planner routa a revision mode
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues per scenario A..G</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cold-start bootstrap | Prima che l'utente veda qualsiasi dato, validate deve confermare la sessione |
| Logout → next login | Stato BLoC di feature deve essere completamente smontato |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Information Disclosure | Account bleed tra logout e login (pitfall #3) | mitigate | MultiBlocProvider di feature spostato DENTRO `authenticatedShell`; `AuthGate` smonta il subtree su `AuthUnauthenticatedShell`; tutti i Cubit dispose automatico. Verificato dal test `AuthGate tears down authenticated child subtree` in task 1 |
| T-01-05-02 | Spoofing | `klimmeck://auth` intercettato da altra app | accept+mitigate | PKCE S256 mitiga l'utilità del code intercepted (D-02 → plan 02). Sul piano OS, per Android l'accept è documentato (non è possibile registrare scheme esclusivo senza App Links). Phase 11 Hardening valuterà App Links / Universal Links |
| T-01-05-03 | Denial of Service | Splash instabile blocca definitivamente l'utente | mitigate | D-18: timer 10s + pulsante "Accedi manualmente" + retry continuo in background. Test cubit `emits NetworkDelayed` verifica il timer |
| T-01-05-04 | Information Disclosure | Firebase init accidentale espone APNs/FCM prima che la config sia in place (pitfall #6) | mitigate | `main.dart` NON chiama `Firebase.initializeApp()`; grep-guard nell'acceptance criteria |
| T-01-05-05 | Tampering | Deep link debug button lasciato in build di produzione | mitigate | Debug button di logout (se aggiunto per testing Scenario C) deve essere rimosso prima di PR — esplicito nell'action del task 2 |
</threat_model>

<verification>
- Full `flutter test` suite verde
- `flutter analyze` pulito
- 7 scenari manuali approvati (checkpoint)
- BACKEND-NOTES.md committato e leggibile
- Branch `feat/auth-session-bootstrap` ready per PR verso `develop`
</verification>

<success_criteria>
- Phase 11 goal raggiunto: "User can log in with their Twitch account, the session persists safely across restarts, and logout fully tears down state"
- Tutti e 7 AUTH-* requirements coperti da test automatici (AUTH-01/03/06 test cubit+service; AUTH-02 grep-guard + storage test; AUTH-04 teardown + widget test; AUTH-05 switchAccount test; AUTH-07 stream translation + UI message test) + 7 scenari manuali
- BACKEND-NOTES.md apre le due Open Questions (connection_init contract, scope) al BE team
- `AuthTokenService` disponibile via `RepositoryProvider` per Phase 2 (Character Creation) e Phase 3 (Real-Time Sync)
</success_criteria>

<output>
After completion, create `.planning/phases/11-auth-session-bootstrap/01-05-SUMMARY.md`
</output>
